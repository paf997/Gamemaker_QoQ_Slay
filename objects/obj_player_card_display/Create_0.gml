card_display = []
isActive = false
function get_cards(){
}

function toggle_active()
{
	show_message($"toggle display:{isActive}{visible}")
	isActive = !(isActive)
	
	show_message($"toggle display:{isActive}{visible}")
}